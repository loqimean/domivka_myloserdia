<%= simple_form_for(situation_photo, url: [:account, situation_photo], html: { class: "contents" }) do |f| %>
  <%= f.input :photo, label: 'Фото', wrapper_class: 'mb-3' %>

  <%= f.submit 'Зберегти', class: 'btn-secondary' %>
<% end %>
